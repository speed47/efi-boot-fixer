# Keep the denser layout this codebase is written in: the default
# heuristics cap chains and struct literals at 60% of max_width, which
# explodes short, readable expressions across five lines.
max_width = 100
use_small_heuristics = "Max"
